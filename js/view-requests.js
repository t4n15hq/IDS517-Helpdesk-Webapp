function searchTickets() {
    // Functionality to search for a specific ticket
    var ticketId = document.getElementById('ticket-id').value;
    alert('Search for ticket ID: ' + ticketId);
}

function goBack() {
    // Go back to the previous page or dashboard
    window.history.back();
}
